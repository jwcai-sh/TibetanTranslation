# 本次完成的任务重点

- 将 AI Vision OCR 改为浏览器端逐行裁图后逐行调用，保存每行的原文坐标。
- 校对视图中每个 block 对应一行原文；原文预览使用该行的精确裁切坐标。
- 增加可重复的文字行检测测试，过滤页面横线等非文本细线。

# 下一步行动

- 用含多行藏文的扫描 PDF 做线上识别验证，确认行检测数量和原文预览裁切符合实际版面。
- 观察长文档逐行调用的耗时和模型限流情况，必要时增加可恢复的逐行进度。

# 需要同步的内容

- 前端 app：`line-layout.js` 在 `app.js` 之前加载；AI Vision 结果以行数组保存，`bbox` 是原文单行坐标。
- 管理后台：无改动。
- 后端服务器：不改 API；现有 `/api/ai-ocr` 接收单行 PNG 与 `block_index`、`block_count` 字段。
- 数据库 / OSS：不改存储路径；保存的项目状态会包含逐行 OCR 结果和坐标。
