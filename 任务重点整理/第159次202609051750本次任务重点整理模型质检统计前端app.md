# 第159次任务重点整理：模型质检统计（前端 app）

## 本次完成

- 每条 AI Vision 标错记录保存模型、服务提供方和识别批次时间。
- 人工保存 AI Vision block 时，保存该 block 的审核字符数与标错字符数。
- 将审核记录写入本地缓存；云端项目的状态同步会一并保存该字段。
- Markdown 校对结果追加“模型质检统计”表，按估算准确率排序。

## 统计口径

- 只统计人工完成审核并点击保存的 block。
- 估算准确率 = 1 - 标错字符数 / 已审核字符数。
- 样本过小时不用于判断模型优劣；需要在相似来源、清晰度和版式的材料上积累足够审核字符后再比较。

## 同步事项

- 前端 app：新增模型级标错元数据与审核统计。
- 后端服务器、数据库、管理后台：无需改接口；现有项目状态 JSON 会保存新增字段。
