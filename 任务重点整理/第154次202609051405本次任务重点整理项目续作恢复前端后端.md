# 第154次本次任务重点整理

## 本次完成

- 修复主页项目续作入口的判断：当前会话仍有源文档时直接回到工作台，不再弹出文件选择框。
- 云端项目缓存保存 `remoteBookId`，后续可按项目恢复。
- 新增云端源文件读取接口，续作时同时恢复 OSS 中的源文件和 OCR/译文状态。
- 本地浏览器重启后仍没有源文件时，改为明确提示原因后再要求重新选择原始文件。
- 新增 IndexedDB 源文件缓存；今后本地项目返回主页后可直接恢复 PDF，不再默认弹出文件选择框。
- 后续第155次将识别链路收敛为 AI Vision-only，BDRC 仅保留历史兼容数据，不再参与新识别。

## 下一步

- 在线部署后，用同一个浏览器测试“主页 -> 继续校对 -> 恢复 PDF、OCR 结果和页码”。
- 如需跨设备浏览项目，需要再增加云端项目列表接口和用户权限控制。
- 旧项目如果此前没有保存源文件，首次仍需重新选择一次；选择后会自动缓存。

## 同步事项

- 前端 app：使用新的项目续作逻辑，保持新建项目仍通过“加载文件”选择源文件。
- 后端服务器：新增 `GET /api/books/{book_id}/source`，从 OSS 返回已上传源文件。
- 管理后台：暂无变更。
- 数据库：暂无变更，仍使用 OSS 中的 `metadata.json` 和 `state.json`。
