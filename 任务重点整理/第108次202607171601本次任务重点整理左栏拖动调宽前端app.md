# 本次任务重点

- 修复 OCR 校对合并视图中原文左栏无法拖动调宽的问题。
- 为校对合并视图增加独立宽度比例，避免与普通三栏视图相互覆盖。
- 拖动计算改为依据当前左右栏实际宽度，并扩大分隔条可操作范围。
- 保留用户调整后的宽度到现有工作区布局缓存。

# 下一步行动

- 前端 app：继续观察不同窗口宽度下的最小栏宽是否符合长时间校对需求。
- 管理后台：本次无变更。
- 后端服务器：本次无变更。
- 数据库：本次无变更；不调整 OCR、AI Vision 或人工校对结果结构。

# 同步事项

- 本次只修改前端布局交互和静态资源版本，不触碰识别、翻译、模型或用户文档数据。
