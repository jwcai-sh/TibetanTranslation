# 第155次本次任务重点整理

## 本次完成

- 当前版本识别按钮直接调用 AI Vision，取消 BDRC 识别前置和回退流程。
- 本地启动脚本不再启动 BDRC OCR 服务。
- 前端隐藏重复的 AI Vision 结果栏，中央栏作为唯一 OCR 结果区。
- 校对视图隐藏 BDRC block、选择项和 BDRC 文案，避免页面继续显示已停用引擎。
- Zeabur 健康信息标记 BDRC 为 disabled，旧 `/api/ocr` 返回 410；AI Vision 接口保持不变。

## 测试与部署顺序

- 先在本地确认页面、AI OCR 健康接口、上传和识别流程。
- 本地确认后再提交并部署 Zeabur。

## 同步事项

- 前端 app：新识别只使用 AI Vision；历史 BDRC 缓存仅作兼容读取。
- 后端服务器：保留源文件、状态、AI OCR、翻译接口；停用 BDRC API。
- 管理后台：暂无变更。
- 数据库：暂无变更。
